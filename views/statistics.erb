<h1> <%= @survey.name %> Statistics </h1>

<ol>
<% @survey.questions.each do |question|%>
  <li><%= question.question %>
    <br>
    <ul>
      <% question.percents.each do |key, value| %>
        <li><%= key %>: <%= value %>%</li>
      <% end %>
    </ul>
  </li>
  <br>
<% end %>
</ol>
